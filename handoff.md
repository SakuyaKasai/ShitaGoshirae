# 論文体裁統一アプリ — 引き継ぎメモ

最終更新: 2026-08-16 / 状態: **本実装 v1.0 完了（テスト187件通過・要ユーザー検証）**

---

## 0. 3行サマリ

- 論文原稿を**日本人間工学会『人間工学』誌 技術報告テンプレート**に合わせたWord原稿へ整えるアプリの受託案件。
- 方式は確定済み: **テンプレ .docx を土台に、本文セクションだけ差し替えて再パッケージする**。ゼロからdocxを組む方式は捨てた。
- 最大の発見: **公式テンプレートがWordスタイルを一切使っていない**（`pStyle` 使用回数 0）。したがって「スタイルを当てる」方式は使えず、**テンプレ自身の直接書式をハンコとして複製する**方式を採る。
- 設計原則は **「作らない、運ぶ、警告する」**（→ 7章）。図・表・数式・参考文献の扱いはすべてこの一文で説明がつく。
- **2026-08-16: 本実装が完了。** 配布物は `index.html` 1ファイル（326KB、テンプレ同梱）。ConoHa WING に置くだけで動く。→ 15章

---

## 1. 案件の背景

| 項目 | 内容 |
|---|---|
| 依頼元 | 友達の友達（研究者）。やりとりは友達経由、および先方のChatGPT「チャぴお」経由 |
| 当初の依頼文 | 「ワードもどきに打ったやつをワードファイルに」「フォーマットバラバラの原稿を希望のフォーマットに」「フォントも揃える」 |
| 真のペイン | スタイル適用をサボって書き上げた原稿に、後から見出し等を割り当てる作業が苦痛 |
| 投稿先 | 日本人間工学会『人間工学』誌 **技術報告** |
| 現況 | 今回の論文は既に6ページのPDFとして完成済み |

### 期待値調整（重要）

**今回の1本は手で直したほうが速い。** アプリの価値は「2本目以降」と「他の人」。ここを先方と握っておかないと「作ったのに今回楽にならない」で終わる。

---

## 2. 先方（チャぴお）から届いた要件

1. 学会テンプレートへの配置（タイトル・著者名・所属・英文抄録・和文抄録・キーワード・本文・図表・参考文献）
2. 書式の統一（用紙・余白・二段組・フォント・文字サイズ・行間・見出し番号）
3. 図表と数式の保持（消えない・崩れない、図表番号と和英題名）
4. 参考文献の整形（学会指定形式へ）
5. 査読用行番号の付与
6. 出力（編集可能なWord + レイアウト確認用PDF）

先方からの譲歩ライン:

- 当面は「**Googleドキュメント原稿を完成見本PDFに近いWordへ変換する**」限定でも十分
- **半自動でよい**（最後は人間が図表位置を微調整）

---

## 3. 資料の状況

| 資料 | 状態 |
|---|---|
| `jes_t_20240401.pdf`（テンプレート兼**執筆要領**、完成見本） | 入手済み |
| `jes_t_20240401.docx`（テンプレ原本） | **入手済み・最重要** |
| 『人間工学』誌投稿規程 2024年4月1日改定版 | 入手済み（テキスト） |
| 現在の原稿（**PDF版**・全6ページ） | 入手済み。ただしPDFのため段落境界が壊れる |
| 現在のGoogleドキュメント原稿（原本） | **未入手** |
| 本文中の図・表の元データ | **未入手**（→ 7-1のとおり、原本docxに埋め込まれていれば流用可） |

※ テンプレPDFに写り込んだWordのタイトルバーが「資料2-1_JES執筆要領」。**テンプレ自体が執筆要領を兼ねている**ため、別紙の執筆要領を探す必要はない。

### 資料同士の突き合わせ結果

食い違いは実質なし。唯一紛らわしいのが**掲載料の別表**（「8ページまで/9ページから」）で、これは全原稿種別共通の料金表であり、技術報告の上限が8ページという意味ではない（技術報告は6ページ目安）。

---

## 4. テンプレート解析結果（この案件の心臓部）

### 4-1. スタイルが使われていない

```
word/document.xml の pStyle 使用回数: 0
段落数: 231 → すべて直接書式
```

`見出し 1` 等のスタイル定義は `styles.xml` に残っているが未使用。しかも定義（Arial / 12pt）と実際の見た目（ＭＳゴシック太字 11pt）が**食い違っている**。

→ **`pStyle` を注入する方式は不可。** テンプレ本文から直接書式を抽出して複製する。

### 4-2. 抽出済みの書式ハンコ

| 要素 | pPr | rPr |
|---|---|---|
| 見出し1（`1. はじめに`） | `<w:spacing w:afterLines="100" w:after="291"/>` | ＭＳゴシック / `<w:b/>` / `sz=22`（11pt） |
| 見出し2（`2-1. 投稿原稿の様式`） | 同上 | ＭＳゴシック + Times New Roman / `<w:b/>` / `sz=20`（10pt） |
| 本文 | `<w:ind w:firstLineChars="100" w:firstLine="195"/>` | ＭＳ明朝 + Times New Roman / `sz=20` |

`docDefaults`: `w:ascii="Century" w:eastAsia="ＭＳ 明朝" w:cs="Times New Roman"`

→ **欧文の既定は Century**。Times New Romanではない。「フォントを揃えたい」の答えはここ。

### 4-3. セクション構成

| | セクション1 | セクション2 |
|---|---|---|
| 内容 | 表題・著者名・抄録・キーワード | 本文以降 |
| 段組 | 1段（`w:cols w:space="425"`） | **2段**（`w:num="2" w:space="824"`） |
| 行番号 | なし | **`<w:lnNumType w:countBy="1"/>`** |
| 余白(twips) | 上1418 / 左右1021 / 下1701 | 上1701 / 左右907 / 下1701 |
| 用紙 | A4（11906 × 16838） | 同左 |
| 種別 | — | `<w:type w:val="continuous"/>` |

→ **セクション1は手つかずで保持し、セクション2の中身だけ差し替える**のが正解。

### 4-4. 規定から拾った数値

- 技術報告: 抄録込み**刷り上がり6ページ以内**、**1ページ ≒ 2000字** → 本文全体で約12,000字
- 英文抄録 200 words 以内 / 和文抄録 400字程度 / キーワード 5個
- 句読点は **「，」「．」**（全角カンマ・ピリオド）
- 数字と単位の間に**半角スペース**、SI単位系
- 図表番号: 図1・表1（英文は Fig. 1・Table 1）、和文・英文の説明を付す
- 参考文献: **SIST02-2007** 準拠、本文中は右肩に番号、出現順
- 投稿の最終形は**PDF**（電子投稿サイトへ提出）。Wordは編集用
- 著者情報（顔写真・プロフィール）は**採択決定後**に提出 → 初回スコープ外

### 4-5. レイアウト完全再現は不要（根拠あり）

テンプレート本文に明記:

> 印刷所にて組版を行うため、本テンプレートの頁数と掲載時の頁数が異なる場合がある

→ **学会側が最終的に組版し直す。** チャぴおが「特に難しそう」と挙げた**改ページ崩れ対策の優先度は低い**。守るべきは構造と書式の規定準拠であって、ピクセル単位のレイアウトではない。

---

## 5. 実現度の色分け

| 要件 | 判定 | 補足 |
|---|---|---|
| 用紙・余白・二段組・行番号 | ◎ | テンプレ継承。実装ゼロ |
| 見出し・本文の書式統一 | ◎ | 価値の中心 |
| 句読点「，．」統一・全角半角正規化 | ◎ | 一括置換。地味に効く |
| 文字数/語数カウンタ（12,000字・200words・400字） | ◎ | 数行で実装可、体感価値大 |
| タイトル・著者・抄録の配置 | ○ | **フォーム入力**で項目を分ける。本文からの自動抽出は誤爆する |
| 画像・表の移設（劣化ゼロ） | ◎ | **PoC検証済み**。ZIP内複製のみ。→ 7-1 |
| 図表番号・和英題名の整形 | ○ | `図1` / `Fig. 1` のキャプション検出は可能 |
| 数式の保持 | ○ | **運ぶだけなら確実**。生成はしない。→ 7-2 |
| 図表の配置（段抜き・段中） | △ | **半自動**。寸法スナップまで自動、最終位置は人間 |
| 改ページ崩れの防止 | × | 原理的に自動保証不可。かつ 4-5 より**優先度低** |
| 参考文献のSIST02整形 | × | ベタ書きからの構造抽出は沼。Zotero等での管理を勧めるほうが早い |

---

## 6. 確定した設計方針

### アーキテクチャ

```
テンプレ .docx を ZIP として開く
  ├ セクション1（表題・抄録）… そのまま保持
  ├ セクション2 … 原稿から生成した段落で差し替え
  │    見出し/本文の判定 → 抽出済みの直接書式ハンコを押す
  └ 再ZIP → .docx 出力
```

**ブラウザ内で完結させる**（`mammoth.js` でdocx読み込み、ZIP操作もJSで可能）。
→ サーバー不要・ホスティング費ゼロ・**未公開論文が外部に出ない**。機密性の問題が消える。

### UI方針

- 左に原文・右にプレビュー。**自動推定の間違いだけクリックで直す**
- **確信度が低い行だけハイライト**し、「要確認 N行」を表示 → 全文チェックが数行チェックになる
- キーボード操作（次の要確認行へジャンプ、数字キーでレベル指定）、一括適用、Undo必須
- Markdown等の記法入力は**前提にしない**（記法を書ける人は最初からスタイルを使う＝対象ユーザーではない）

### ホスティング

静的ホスティングで十分。**ConoHa WING（既存契約）にHTMLを置くだけでよい**。Cloudflare Pages / Netlify / GitHub Pages も可。Streamlitは不適（サーバー常駐・原稿が外に出る・スリープ問題）。

---

## 7. 設計原則:「作らない、運ぶ、警告する」

**アプリは新しいものを生成しない。入力にあるものをそのまま運び、運べないものは警告する。**
図・表・数式・参考文献の扱いは、すべてこの一文から導ける。生成処理が存在しなければ、崩れようがない。

| 要素 | 扱い |
|---|---|
| 本文・見出し | 運ぶ + テンプレの書式ハンコを押す |
| 画像 | 運ぶ（バイナリをZIP内で複製） |
| 表 | 運ぶ（`w:tbl` のXMLごと複製、列幅のみ調整） |
| 数式 | 運ぶ（`m:oMath` のXMLごと複製）。無ければ**警告** |
| 参考文献 | 運ぶ + 規定との差分を**警告**（自動整形はしない） |
| 図表の最終位置 | 運ばない。人間が調整 |

### 7-1. 画像の移設仕様（PoC検証済み）

手順は3つだけ:

1. 入力docxの `word/media/` から画像を出力docxへ**そのままコピー**
2. `word/_rels/document.xml.rels` に `Type=".../image"` のリレーションを追加
3. 本文に `<w:drawing><wp:inline>` で参照し、**拡大率（`wp:extent` の cx/cy）だけ再計算**

再エンコードが発生しないため、**解像度・画質は元ファイルと完全に同一**。規定の「印刷後に明瞭に読める解像度」を自動的に満たす。

**テンプレの2段組から逆算した確定寸法:**

| | twips | EMU | mm |
|---|---|---|---|
| 片段横寸法 | 4,634 | 2,942,590 | **81.7** |
| 段抜き横寸法 | 10,092 | 6,408,420 | **178.0** |

規定が「片段または段抜きのいずれか」と指定しているため、**この2値にスナップさせる**。

**落とし穴:**

- **Wordの図形・SmartArt・グラフで描かれた図は `media/` に入らない**（DrawingMLとして描画される）。→ 画像として貼り直してもらう（警告W14）
- 画像がEMF/WMFの場合、Word以外での再現性が落ちる
- フローティング（`wp:anchor`）配置だと段落との紐付けが緩い。→ **出現順**と本文中のキャプション行（`図1．`）を手がかりに対応づける

### 7-2. 数式の方針

**アプリでは数式を生成しない。** 原稿側でWordの数式ツール（`m:oMath`）を使ってもらい、そのXMLを移設する。

現行原稿はこの条件を**満たしていない**（Unicodeの下付き文字でベタ書き）:

```
ΣᵢFᵢ＝0     F＝∫Sf(s)dS     M(r)＝(rL－r)×F
```

→ フォント依存で化ける。アプリの役割は**検出と警告**（W07）。現行原稿の4本は、手で数式ツールに入れ直すのが最速（実測5分未満の作業量）。

---

### 7-3. 英文タイトル・英文著者名の規定

テンプレ本文に明記されている、**機械的に判定できる数少ないルール**:

- 原則、**各単語の頭文字を大文字**にする
- ただし**冠詞**（a, the）、**接続詞**（and, or, but, not, for, if, while, since など）、**前置詞・不定詞**（at, to など）は**頭文字を小文字**にする
- **英文氏名は姓（Family name）のみ全て大文字**で記載する
- 書式は **9pt / Times New Roman / Bold**（テンプレ見本 "English Title Is 9 Points, Times New Roman Bold" より）

**実装上の注意（規定に書かれていない例外）:**

1. **先頭語は冠詞でも大文字**。規定を字義どおり適用すると `A Conversion…` の `A` が小文字になってしまうが、テンプレ見本自体が先頭大文字。→ 「先頭語は常に大文字」を例外として実装し、警告は出さない
2. **ハイフン語**（`Waist-Cord`）の後半をどうするかは規定に記載なし。→ 慣行に従い両方大文字。ただし警告は出さず、💡情報レベルに留める
3. ストップワード一覧は規定の列挙に「など」が付いているため、**閉じた集合ではない**。実装は代表的な語（a, an, the, and, or, but, nor, for, if, while, since, as, at, by, in, of, on, to, from, with, into, over, than, that, when）で始めて、運用で追加

**現行原稿の判定結果: ✅ 完全準拠。**

```
A Conversion Method from Women's Kimono Waist-Cord Dressing
Structures to Numerical Analysis Conditions
   → from / to が小文字、他は大文字。規定どおり
Yuri AOKI, Miho KASAI  → 姓のみ全大文字。規定どおり
```

---

## 8. セクション1（表題・抄録エリア）の自動抽出

**方針: 最終的にはGUIフォームで受け取る。ただし抽出を試みて候補をプリセットし、人間が確認・修正する。**
既存UIの「推定して、間違いだけ直させる」思想と同じ。空欄から埋めるより、直すほうが速い。

### 8-1. 抽出アルゴリズム（実データで検証済み）

決め手は **ASCII比率・文字数・出現順序・アンカー文字列**の4つ。上から確実な順:

| フィールド | 手がかり | 確信度 |
|---|---|---|
| キーワード | `キーワード：` 〜 `）` を「，」で分割 | **高** |
| 英文タイトル | 冒頭付近で ASCII比率 > 0.7 かつ **`, by ` を含む**ブロック。`, by ` の前がタイトル | **高** |
| 英文著者名 | 同ブロックの `, by ` 以降を `&` / `and` で分割 | **高** |
| 姓の判定 | 分割後の語のうち**全て大文字の語＝姓**（規定そのものが判定器になる） | **高** |
| 英文抄録 | ASCII比率 > 0.85 かつ 120〜280 words のブロック | **高** |
| 和文抄録 | キーワード行の**直前**にある300〜500字のブロック | 中 |
| 和文タイトル | 冒頭付近・ASCII比率 < 0.3・**句点で終わらない**・6〜45字の行 | 中 |
| 和文著者名 | 和文タイトルの直後で、全角スペースまたは「，」区切りの2〜5字の語が並ぶ行 | 中 |
| 所属 | 脚注番号付き、または「大学」「学部」「株式会社」を含む行 | 中 |

**「規定そのものが判定器になる」**のがこの案件の面白いところ。「姓は全て大文字」という規定があるおかげで、`Yuri AOKI` から姓を機械的に取り出せる。

### 8-2. 実データでの抽出結果（現行原稿・PDF版）

```
① キーワード     [高] 5個 → 着物, 着付け, 腰紐, 荷重流路, 静力学
② 英文タイトル   [高] A Conversion Method from Women's Kimono…
③ 英文著者名     [高] Yuri AOKI / Miho KASAI（姓: AOKI, KASAI）
④ 英文抄録       [高] 201 words   ← 規定200 words以内をわずかに超過
⑤ 和文抄録       [中] 誤検出（PDFのブロック分割が崩れたため）
⑥ 和文タイトル   [中] 女性腰紐着付け構造の数値解析条件への変換手法
```

**①〜④・⑥は一発で当たった。⑤の失敗はPDF入力に起因する**（段落境界が壊れているため）。docx原本なら段落が明示的に区切られているので、この誤検出は起きない可能性が高い。→ **原本を要求すべき理由がまた1つ増えた。**

なお副産物として、**英文抄録が201 wordsで規定の200 wordsをわずかに超えている**ことが判明（規定は "approximately" なので 💡 情報レベル）。

### 8-3. UI設計

- 抽出結果を**フォームにプリセット**し、確信度に応じて色分け（高=そのまま／中=要確認ハイライト／失敗=空欄+フォーカス）
- **自動確定しない。** 送信前に必ず人間の目を通す
- **2回目以降は前回値を記憶**する。著者名・所属・連絡先はほぼ不変なので、これだけで入力の大半が消える（実装コストはほぼゼロ、効果は大）

---

## 9. 取扱説明書に書くこと

### 9-1. 守ってもらう前提（これが崩れると出力が壊れる）

1. **原稿は Word または Googleドキュメント**（.docx書き出し）で渡す。**PDFは不可**（段落境界が失われるため）
2. **数式はWordの数式ツールで書く**。`ᵢ` `∫` `Σ` などのUnicode文字で代用しない
3. **図は画像として貼る**。Wordの図形・SmartArtで描かない
4. 図は**片段81.7mm / 段抜き178mm**で印刷して読める解像度で作る
5. **見出しには必ず番号を付ける**（`1.` / `2-1.` / `2-1-1.`）。番号がない行は本文と区別できない
6. **タイトル・著者名・所属・抄録・キーワードは専用フォームに入力**する。本文に混ぜない
7. 参考文献は本文末に「文献」見出し以降へまとめる

### 9-2. アプリがやらないこと（非対応の明示）

- 参考文献のSIST02形式への**自動整形**（差分の指摘のみ）
- 見出し番号の**自動振り直し**（重複・欠番の警告のみ）
- 図表の**最終的な配置**（プレースホルダと寸法スナップまで）
- **改ページ位置の保証**（学会側で組版し直されるため、そもそも不要 → 4-5）

### 9-3. 出力後に人間がやること

1. 図表の位置を段の切れ目に合わせて微調整
2. 警告リストの各項目を確認・修正
3. 数式の表示崩れがないか目視
4. PDFに書き出して電子投稿サイトへ提出

---

## 10. エラー・警告カタログ

### 10-1. メッセージの書き方

- **責めない。** 「間違っています」ではなく「〜してください」
- **原因と次の一手をセットで書く。** 何が起きたかだけ言われても人は動けない
- **件数を出す。** 「93箇所」と分かれば、手作業か一括かを本人が判断できる
- 自動修正できるものは、**修正前に見せてから実行**する（勝手に直さない）

重大度は3段階: 🛑 停止（処理を続けられない）／ ⚠️ 警告（出力はするが要対応）／ 💡 情報（参考）

### 10-2. カタログ

| ID | 検出条件 | 重大度 | メッセージ案 | 自動修正 |
|---|---|---|---|---|
| E01 | 入力がPDF | 🛑 | PDFは段落の区切りが失われます。WordまたはGoogleドキュメントの原本をお使いください | — |
| E02 | テンプレのセクション構成が想定と異なる | 🛑 | このテンプレートは対応していない構成です。学会配布の最新版をご確認ください | — |
| E03 | 1行目が番号付き＝タイトル未検出 | ⚠️ | タイトルが見つかりません。タイトルにする行を指定してください | 手動指定UI |
| W01 | 句点「。」が混在 | ⚠️ | 句点が「。」の箇所が **N件** あります。規定では「．」です | **可** |
| W02 | 読点「、」が混在 | ⚠️ | 読点が「、」の箇所が N件 あります。規定では「，」です | **可** |
| W03 | 見出し番号の記法ゆれ（全角「．」／半角混在） | ⚠️ | 見出し番号の書き方が揃っていません（例: `1．` と `2-1.`） | **可** |
| W04 | 見出し番号の重複・欠番 | ⚠️ | 番号 `1-3-1` が2回使われています。振り直しますか？ | 確認後のみ |
| W05 | 参考文献リストにあるが本文中に引用がない | ⚠️ | 文献 N) が本文から引用されていません | — |
| W06 | 本文中の引用番号に対応する文献がない | ⚠️ | 本文の N) に対応する文献がリストにありません | — |
| W07 | Unicodeの数式文字（`ᵢ` `∫` `Σ` `×` 等）を検出 | ⚠️ | この行は数式の可能性があります。Wordの数式ツールで作成してください | — |
| W08 | 本文が約12,000字（6ページ相当）を超過 | ⚠️ | 本文が N字です。6ページ目安の約12,000字を超えています | — |
| W09 | 英文抄録が200 wordsを超過／和文抄録が400字から大きく外れる | ⚠️ | 英文抄録が N words です（規定200 words以内） | — |
| W10 | キーワードが5個でない | 💡 | キーワードが N個です（見本は5個） | — |
| W11 | 図表番号の連番飛び／英文題名（`Fig. 1`）がない | ⚠️ | 図2の英文題名が見つかりません | — |
| W12 | 図の実解像度が片段81.7mmで150dpi未満 | ⚠️ | 図Nの解像度が不足しています。印刷時に不鮮明になる可能性があります | — |
| W13 | 参考文献がSIST02形式と異なる | 💡 | 文献Nの区切りが規定と異なります（`：` → `．`、出版地の記載など） | — |
| W14 | 図がWord図形／SmartArtで `media/` に画像がない | ⚠️ | 図Nは画像として貼り直してください（図形のままでは移設できません） | — |
| W15 | 数字と単位の間に半角スペースがない | 💡 | `32kHz` → `32 kHz` の形が規定です | **可** |
| W16 | 英文タイトルのキャピタライゼーションが規定と異なる | ⚠️ | 英文タイトルの `Of` は規定では小文字です（前置詞） | **可** |
| W17 | 英文著者名の姓が全て大文字になっていない | ⚠️ | 英文氏名は姓のみ全て大文字で記載します（例: Yuri AOKI） | **可** |
| W18 | 英文タイトルがハイフン語を含む | 💡 | `Waist-Cord` のハイフン後の大文字化は規定に記載がありません。現状のままにします | — |
| I01 | 本文段落に一字下げがない | 💡 | 段落の字下げを付けました | **可（既定ON）** |

### 10-3. 実データでの検出実績（現行原稿6ページ）

PDF版に対して機械チェックを実行した結果:

| 項目 | 結果 |
|---|---|
| 句点「。」（W01） | **93箇所**（規定違反） |
| 読点「，」 | 255箇所すべて規定どおり ✅ |
| 本文文字数 | 7,953字（12,000字枠に余裕） |
| 未引用文献（W05） | **文献3) が本文から引用されていない** |
| 見出し記法ゆれ（W03） | 大見出し `1．`（全角）／小見出し `2-1.`（半角） |
| 参考文献形式（W13） | SIST02非準拠（`：` 区切り、出版地なし） |
| 英文タイトル（W16） | **✅ 準拠**（from / to が小文字、他は大文字） |
| 英文著者名（W17） | **✅ 準拠**（AOKI / KASAI とも姓が全大文字） |
| 英文抄録の語数（W09） | 201 words（規定は "approximately 200 words"）→ 💡 |

**読点は255箇所すべて正しいのに句点だけ93箇所落ちている。** 執筆者は規定を理解した上で変換を忘れただけ。W01の一括修正だけで、この原稿の最大の指摘が消える。**チェッカー機能がMVPの目玉になり得る**ことの実証。

---

## 11. 検証済みの技術メモ

- **ブラウザ内docx生成**は無圧縮ZIP（store）を手組み + OOXML直書きで実現可能。`docx` ライブラリ不要。CRC32・ローカルヘッダ・中央ディレクトリ・EOCDを自前で書く
- **和欧混植**は `<w:rFonts w:ascii="欧文" w:hAnsi="欧文" w:eastAsia="和文" w:hint="eastAsia"/>`。1つのスタイル内でWordが自動的に切り替える。ユーザーが「英数字だけ選択して変更」する作業から解放される — **ここが「フォントを揃えたい」の本丸**
- **artifact内のダウンロードはブロックされる**ことがある（サンドボックスiframe、特にモバイル）。→ 自動クリックではなく**実リンクをタップさせる**方式にする。本番の静的ホスティングでは発生しない
- テンプレ流し込みPoC（`jes_yukimaru.docx`）で、2段組・行番号・抄録欄の継承を**確認済み**

---

## 12. 実装の現状（v1.0）

### 12-1. モジュール構成

| ファイル | 責務 | 行数 |
|---|---|---|
| `src/xml-util.js` | XMLのバランス取り出し・属性・エスケープ | 約200 |
| `src/source-parse.js` | 原稿docx → ブロック列（表・数式・画像は生のまま保持） | 約260 |
| `src/extract-meta.js` | 表題・著者・抄録・キーワードの推定（→ 8章） | 約230 |
| `src/heading-detect.js` | 見出し判定・番号正規化・W03/W04/W29/W30 | 約280 |
| `src/checker.js` | W01/W02/W05〜W07/W10/W15〜W18/W22 と自動修正 | 約500 |
| `src/image-transfer.js` | 画像移設・rId/docPr採番・寸法スナップ | 約430 |
| `src/numbering-merge.js` | 番号定義のマージとオフセット再採番 | 約235 |
| `src/style-import.js` | 原稿から表スタイル定義を運ぶ | 約110 |
| `src/compose.js` | ハンコ押し・表の幅調整・所属差し替え | 約480 |
| `src/pipeline.js` | 変換の一本道 | 約240 |
| `src/app.js` / `app.css` | 画面（ゲラ見立て） | 約520 |
| `src/manifest.json` | **テンプレ固有値を全外出し** | 生成物 |

補助ツール: `tools/build-manifest.mjs`（テンプレ→manifest）/ `tools/build-bundle.mjs`（単一HTML生成）/ `tools/make-fixture*.py`（検証用原稿の生成）

### 12-2. manifest による外部化

テンプレ固有の値はコードに一切埋め込んでいない。学会がテンプレを改訂したら **`npm run manifest` を再実行するだけ**でよい。

manifest が持つもの: `w:document` のルート属性（名前空間宣言）／セクション境界（**インデックス決め打ちではなく sectPr を持つ段落を探して求める**）／両セクションの sectPr ／段組から導いた寸法／6種のハンコ（pPr・rPr）／セクション1の6スロット定義／所属テキストボックスの run 丸ごと／参考文献の numId ／各種上限値

### 12-3. 12章（旧）の未実装項目 — 全て解決

| 旧課題 | 状態 |
|---|---|
| エラー・警告カタログの実装 | ✅ W01〜W33 を実装。自動修正はトグルで選択 |
| 番号の重複・飛びの検出 | ✅ W04。同階層の連番と階層飛ばし（W29）も |
| 参考文献以降の見出し判定停止 | ✅ `文　献` 検出で停止。`1)` の誤爆を回避 |
| 箇条書きの扱い | ✅ 記号は残し、字下げを外した本文として運ぶ |
| 図表キャプション専用書式 | ✅ `parseCaption` で判定し中央揃え |
| 見出しの入れ子ツリー化 | ➖ ゲラ表示に変更したため不要になった |
| 番号の自動振り直し | ⏸ 警告のみ（要ヒアリング。→ 13章） |

### 12-4. テスト

```
既存4本            86 passed   （EMU修正後も維持）
source-parse       29 passed
compose / pipeline 52 passed
ブラウザ通し       20 passed   （実Chromium・E2E）
────────────────────────────
合計              187 passed / 0 failed
```

`npm test` で全部走る。テンプレの展開と検証用原稿の生成もランナーが面倒を見る。

**加えて、生成した .docx を毎回 XSD で検証している**（`office/validate.py`）。Word は不正なXMLに対して「読み取り不能」としか言わないため、機械検証がないと原因特定に時間が溶ける。実際 `w:drawing` の配置ミスはこれで一発で出た。

---

## 13. 未決事項 / 次アクション

**先方に依頼中（実装完了により、いずれもアプリ側で検出・案内できるようになった）:**

1. **Googleドキュメント原稿の原本**（PDFではなく。→ E01。アプリが拡張子を見て案内する）
2. 図がWord図形／SmartArtで描かれていないかの確認（→ W14。**自動検出できる**）
3. 数式のWord数式ツールへの入れ直し（→ W07。**自動検出できる**）

**次にやること:**

1. **実原稿での検証**（最優先）。検証はすべて自作の fixture で行っており、実際の原稿はまだ通していない
2. Windows + Word 2016/2019 での目視確認と PDF 書き出し（Mac には ＭＳ明朝・ＭＳゴシック・メイリオが無い）
3. ConoHa WING へ `index.html` を配置

**ヒアリングで確認すべきこと:**

- 原稿は**論文全体を貼るのか、章単位で貼るのか**（章単位なら「1行目＝タイトル」ルール自体を捨て、タイトル欄を別に設けるべき）
- 番号の自動振り直しは欲しいか
- 数式をどうやって作っているか（Word数式 / LaTeX / 画像貼り付け）
- 図表はどうやって作っているか（Canva画像なら**画像内の文字はピクセルなので後から変更不可**。テンプレ配布かSVG化か警告のみか、方針の合意が必要）
- 投稿先は毎回この学会か、複数誌に出すか（**ここでコストが一桁変わる**）

---

## 14. プロジェクト概要欄に貼る用の短文

> 論文原稿を日本人間工学会『人間工学』誌 技術報告テンプレートに合わせたWord原稿へ整えるWebアプリの受託案件。テンプレ .docx を土台に差し替える方式で、ブラウザ内完結（原稿を外部送信しない）。本実装 v1.0 完了・実原稿での検証待ち。テンプレートはWordスタイルを使用しておらず全段落が直接書式のため、テンプレ本文から抽出した書式を複製する設計を採る。テンプレ固有値は manifest.json に外出ししており、学会の改訂には抽出ツールの再実行だけで追随できる。

---

## 15. 実装で分かったこと（v1.0）

要件定義の段階では見えていなかった落とし穴。**いずれも Word がエラーを出さずに黙って壊れる**タイプで、機械検証がなければ本番で発覚していた。

### 15-1. `w:drawing` は段落直下に置けない

`<w:p><w:drawing>` は不正。必ず `<w:p><w:r><w:drawing>` にする。
XSD検証で一発で出たが、Word は「読み取り不能」としか言わない。

### 15-2. 参考文献の numId は 17。ただし素朴に探すと 14 を掴む

`numPr` を持つ段落はテンプレ内に他にもある（`1) 新しい発見または知見の提示` 等の箇条書き＝numId 14）。
**「文　献」見出し以降にある numPr 段落**に限定して初めて 17 が得られる。

### 15-3. 所属テキストボックスのアンカーは付け替えが必要

テンプレの指定は `positionV relativeFrom="paragraph" posOffset="379730"`（アンカー段落の10.5mm下）。
テンプレでは本文4段落目に付いているため**偶然**ページ下端に落ちているだけで、原稿を差し替えると本文の途中に浮く。
→ `relativeFrom="page"` の絶対座標へ付け替える。オフセットは `(pgSz.h − pgMar.bottom) × 635 − extent.cy`。

**`bottomMargin` 基準は使ってはいけない。** Word は解釈するが LibreOffice は図形グループの子要素を取りこぼし、**区切り線だけ残ってテキストが消える**（実測）。

さらに **VML fallback 側には `mso-position-vertical-relative` が存在しない**（既定は段落基準）。置換ではなく挿入しないと、Word 2007 でだけ位置がずれる。

### 15-4. 表の罫線は「作らず運ぶ」

原稿の表が `<w:tblStyle w:val="TableGrid"/>` を参照していても、テンプレの styles.xml には styleId が `"1"`〜`"45"` の数値IDしか無いため参照が解決できず、**罫線が丸ごと消える**。

→ 罫線を引き直す（＝作る）のではなく、原稿の styles.xml から定義を運ぶ。`basedOn` の連鎖も辿らないと罫線が半分になる。段落スタイルは運ばない（テンプレのハンコで上書きするのが本アプリの仕事のため）。

### 15-5. 非貪欲な正規表現で `w:r` を切ってはいけない

`/<w:r[^>]*>[\s\S]*?<\/w:r>/` は、所属テキストボックスのように **`w:r` の中に `txbxContent` → `w:r` が入れ子になる**ケースで最初の `</w:r>` で切れる。
manifest 抽出でこれを踏み、テキストボックスの検出が 0 件になった。→ 深さを数えるバランス取り出しが要る。

### 15-6. 片段幅の EMU は twips から直接求める

mm を経由すると丸め位置が変わる（81.7mm → 2,941,200 EMU）。

```
段抜き = 11906 − 907 − 907        = 10092 twips
片段   = (10092 − 824) / 2        =  4634 twips
       = 4634 × 635               = 2,942,590 EMU (81.739 mm)
```

### 15-7. 抽出は「書式シグナル」で引く。テキスト一致は壊れる

和文抄録の本文には「キーワード」という語が含まれるため、素朴な部分一致だとキーワード欄と取り違える。
→ `sz=28` なら和文タイトル、`jc=right` なら和文著者名、というように**書式で引く**。テキストは従。

### 15-8. 自動抽出の実績

`fixture-full.docx`（テンプレ見本を上書きして書いた想定の原稿）に対し、**9項目すべて一発で当たった**。
姓の抽出（`Yuri AOKI` → `AOKI`）まで機械的に取れる。**規定そのものが判定器になる**という 8-1 の見立ては正しかった。

---

## 16. 運用

### 16-1. 配布

`index.html` 1ファイル（326KB）をサーバに置くだけ。テンプレ .docx は base64 で同梱しており、外部への通信は一切発生しない。**原稿がブラウザの外に出ない**ことが、未公開論文を扱ううえでの前提。

Google Fonts のみ CDN から読むが、読めなくても代替フォントで動く。

### 16-2. テンプレが改訂されたとき

```bash
# 新しいテンプレを template.docx として置いてから
npm run manifest   # テンプレ → manifest.json
npm run build      # → index.html
npm test           # 187件
```

コードの修正は原則不要。ただし `build-manifest.mjs` はセクション構成が現行と同じ（1段組の表題部 → 2段組の本文部）であることを前提にしているため、構成が変わった場合は E02 で止まる。

### 16-3. 出力後に人間がやること

1. 図表の位置を段の切れ目に合わせて微調整
2. 指摘リストの各項目を確認・修正
3. 数式の表示崩れがないか目視
4. **Windows で** PDF に書き出して電子投稿サイトへ提出（Mac には ＭＳ明朝・ＭＳゴシック・メイリオが無い）
